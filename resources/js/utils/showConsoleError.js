/**
 * Utils function to print errors in the console
 * @param {any} props - The props are pass directly to the console.error function
 */
export default function showConsoleError(props) {
    /* eslint-disable-next-line no-console */
    console.error(props);
}
